(Side note for later: the in-kernel ch341 driver handles this same chip without that bug. Swapping the vendor ch34x driver for the built-in ch341 would silence the warning. Optional cleanup, not needed to get rolling.)

